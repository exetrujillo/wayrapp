/**
 * Mock Service Worker (MSW) handlers for API mocking
 * Re-exports handlers from the main mocks directory for backward compatibility
 */

export { handlers, mockApiResponses } from '../../mocks/handlers';

